feat: clickable avatar upload in profile — no more URL input

- Replace Avatar URL text input with a clickable avatar in the profile header
- Click the avatar (or the "Upload Profile Photo" button) to select a file
- File is uploaded to /api/upload and the URL is stored in a hidden field
- Shows "Change" overlay on hover and "Uploading..." while in progress
- Validates file type (JPG/PNG/WebP/GIF) and size (5 MB max) client-side
- Toast confirms upload, user clicks Save Profile to persist

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
