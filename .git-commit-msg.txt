feat: add student suspend/delete, super admin sees all students, promote/demote instructors

- Add status field to User schema (ACTIVE/SUSPENDED)
- Add suspendStudentAction, unsuspendStudentAction, deleteStudentAction
- Suspended users are blocked from logging in with a clear error message
- Super admin (yeboahmichael) now sees ALL students in the system, not just
  those enrolled in their own courses
- Student table shows status badge (Active/Suspended) with color coding
- Each student row has Suspend/Reactivate and Delete buttons with confirmations
- Add promoteToInstructorAction — promotes a STUDENT to SUPER_ADMIN
- Add demoteInstructorAction — demotes a SUPER_ADMIN back to STUDENT
- Instructors page has "Promote Student" button showing all active students
- Instructors page has "Demote" button (with confirmation) for non-self instructors

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
